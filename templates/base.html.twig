<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
        <img src="C:/xampp/htdocs/Projet_WebServeur/Projet/images/image.jpg" alt="">
        <p>
            {% if app.user %}

            Bonjour {{ app.user.nom }} - <a href="{{ path('app_user_profile') }}">Modifier mon profil</a> - <a href="{{ path('app_logout') }}">Se déconnecter</a>
            
            {% else %}
                On se connaît ? <a href="{{ path('app_login') }}">Se connecter</a>

            {% endif %}
            
        </p>
        {% block body %}{% endblock %}
    </body>
</html>
