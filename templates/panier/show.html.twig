{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}
    <h1>Panier</h1>

    <table class="table">
        <tbody>
            <tr>

                <th>Id</th>
                <th>Vins </th>
                <th>Prix</th>
                <th>Quantité</th>
                <th>Actions</th>
            </tr>

            {% for v in panier.vin %}

                <td>{{ panier.id }}</td>
                <td>{{ v.nom }}</td>
                <td>{{v.prix}}</td>
                <td>TODO</td>
                <td><button>supprimer</button></td>
                <td><button>+1</button></td>
                <td><button>-1</button></td>
                {% endfor %}
        </tbody>
    </table>

{% endblock %}
